meta {
  name: update-product
  type: http
  seq: 4
}

patch {
  url: {{entityUrl}}?id=eq.{{id}}
  body: json
  auth: inherit
}

params:query {
  id: eq.{{id}}
}

body:json {
  {
    "name": "Updated Product for {{email}}"
  }
}

vars:pre-request {
  id: 
}

settings {
  encodeUrl: true
  timeout: 0
}
