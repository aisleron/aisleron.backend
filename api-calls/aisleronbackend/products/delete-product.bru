meta {
  name: delete-product
  type: http
  seq: 2
}

delete {
  url: {{entityUrl}}?id=eq.{{id}}
  body: none
  auth: inherit
}

params:query {
  id: eq.{{id}}
}

vars:pre-request {
  id: 
}

settings {
  encodeUrl: true
  timeout: 0
}
